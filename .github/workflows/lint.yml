on: push

jobs:
    ruff-check:
        runs-on: ubuntu-latest
        steps:
            - uses: astral-sh/ruff-action@v4.1.0
            with: 
                src: >- 
                    ../my_file.py


 